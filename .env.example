NODE_ENV=development
PORT=4000
WEB_ORIGIN=http://localhost:5173
VITE_API_URL=http://localhost:4000/api/v1
DATABASE_URL=postgresql://yersps:yersps@localhost:5432/yersps
JWT_ACCESS_SECRET=replace-with-at-least-32-random-characters
ACCESS_TOKEN_TTL_MINUTES=15
REFRESH_TOKEN_TTL_DAYS=7

# Optional deterministic development seed passwords. When omitted, the seed command generates
# strong one-time passwords and prints them to the terminal.
SEED_ENTREPRENEUR_PASSWORD=
SEED_ADMIN_PASSWORD=
SEED_SYSTEM_ADMIN_PASSWORD=
